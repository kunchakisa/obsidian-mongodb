It is a [database](Database.md), storing data in a structured manner, but without rows and columns. The traditional way was to use tables of dataset with rows of data and columns of values or properties.